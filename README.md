# Secure-Banking-System
Enterprise core banking terminal using Java, Spring Boot, and SQL (H2/JPA). Features safe client onboarding and custom server-side data logic. Bypasses framework wrappers to implement a manual Insertion Sort loop to rank INR balances, and a Binary Search engine for high-speed lookups. Includes an async vanilla JS frontend dashboard.
